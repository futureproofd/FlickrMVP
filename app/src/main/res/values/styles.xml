<resources>
    <!-- Base application theme. This also applies to elements such as the ProgressBar-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/toolBarPrimary</item>
        <item name="colorPrimaryDark">@color/toolBarDark</item>
        <item name="colorAccent">@color/tabScroll</item>
    </style>

    <style name="ToolBarTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textStyle">bold</item>
    </style>

</resources>
